step 1. pip install -r requirements.txt
step 2. setup ur open router api account generate an API key and paste it in environment file in both the variables and then follow step 3
step 3. streamlit run app.py
